# 砚砚（YanYan）项目全面审查报告

> 审查日期：2026-08-15 ｜ 范围：`src/`（Vue 3 前端）＋ `src-tauri/`（Rust 后端）＋ 配置/构建/仓库卫生 ｜ 版本 0.3.0
> 方法：三路并行深度代码审查（Rust 后端 / 前端逻辑层 / UI 组件层）+ 构建、测试、类型检查实测验证。

## 0. 验证结果（实测）

| 检查项 | 结果 |
| --- | --- |
| `vue-tsc --noEmit` 类型检查 | ✅ 通过 |
| `vite build` 生产构建 | ✅ 通过（唯一警告：主 chunk 700KB / gzip 247KB，超过 500KB 阈值） |
| `vitest run` 单元测试 | ✅ 13/13 通过（仅 2 个测试文件：useDiff、countWords） |
| `cargo check` 后端编译 | ✅ 通过（增量 0.92s） |
| GitHub Actions CI | 存在 `.github/workflows/ci.yml`（前端 ubuntu / 后端 windows），但**尚未提交**（untracked） |

**结论：当前工作树可编译、可构建、测试全绿，无构建层面缺陷。**

## 1. 发现汇总

| 级别 | Rust 后端 | 前端逻辑层 | UI 组件层 | 仓库卫生 |
| --- | --- | --- | --- | --- |
| Critical | 2 | 0 | 0 | 0 |
| High | 3 | 2 | 3 | 1 |
| Medium | 7 | 7 | 8 | 4 |
| Low | 6 | 11 | 8 | 3 |

---

## 2. Critical

### C1. IPC 命令面无路径沙箱 —— 任意文件读写/删除/穿越（Rust）

`src-tauri/src/lib.rs` 中 `read_file`、`write_file`、`create_dir`、`create_file`、`delete_file`、`move_path`、`rename_path`、`read_file_tail`、`export_project`、`export_epub`、`list_backups` 等**所有接受路径的命令都直接把前端传入的 `path` 交给 `std::fs`**，无 `project_path` 约束、无路径规范化、无符号链接检测（已抽查验证属实）。`project_path` 存在 config 中，但只有字数索引用它做过校验。

```rust
#[tauri::command]
fn read_file(path: String) -> Result<String, String> {
    fs::read_to_string(&path).map_err(|e| e.to_string())  // 任意路径可读
}
```

**影响**：一旦前端出现任何 XSS 或渲染注入（例如小说正文/章节名/AI 输出被当作 HTML 处理），即可升级为任意文件读写删除。当前靠「自研前端 + CSP `script-src 'self'`」兜底，属裸奔状态。

**修复**：实现统一 `resolve_within(root, path)` 沙箱函数（canonicalize 后 `starts_with` 校验），所有命令强制走它；`project_root` 从后端 config 读取，不信任前端传参。

### C2. `delete_path` 兜底逻辑可把文件移出项目（Rust）

`project_root` 未传时向上盲走 3 层作为 `.trash` 根：路径深度不足（如 `C:\a\b.md`）会把用户文件 `rename` 到系统盘根下的 `.trash`。注释声称「固定放项目根」但实现不保证。**修复**：完全依赖后端 config 中的 `project_path`，删除命令不接受前端传入 root。

---

## 3. High

### H-1. 自动保存竞态 —— 保存期间键入的内容被静默丢弃（前端逻辑层，数据丢失）

`src/stores/file.ts` `handleSave()`（约 L550）：

```ts
const content = getLatestContent(openFile.value.path)   // ① 同步快照 "abcd"
await api.writeFile(openFile.value.path, content)       // ② 异步写盘，期间用户键入 "e"
updateActiveFile({ content, isDirty: false })           // ③ 用旧快照覆盖 store → "e" 丢失
```

写盘期间的新键入被旧快照回写覆盖，MdEditor 的 props watcher 随后把编辑器整篇替换回旧内容。大文件/慢盘上间歇性复现的丢字 bug。**修复**：写盘后重新 `getLatestContent` 比对，不一致则保持 `isDirty: true` 并再次保存；props watcher 跳过「编辑器有未落盘本地编辑」时的整篇替换。

### H-2. 保存失败仍关闭文件/标签，脏内容丢失（前端逻辑层）

`file.ts` `closeActiveTab()` 与 `EditorTabs.vue` `closeTab()` 调用 `await saveFile(path)` 后**忽略返回值**，失败（磁盘满/占用）也照样 `closeFile`，唯一内存副本随之销毁。**修复**：检查返回值，失败时提示并中止关闭。

### H-1′. MdEditor 异步生命周期竞态（UI 层）

快速切换标签时：`watch(() => props.path)` 的异步回调无竞态守卫，A→B→C 切换会把 A 的 doc 缓存到 B 的 `stateCache`、把 A 的内容写进 B 的 undo 文件；`onMounted` 中 `await loadSavedState` 返回前组件被卸载则 EditorView 永不销毁。**修复**：引入切换令牌（switch token），`await` 后校验令牌与 `view.destroyed`。

### H-2′. 流式输出每 token 重渲染全部历史消息（UI 层，性能）

`messageHtml()` 在模板内对每条历史消息执行 `renderMarkdown` + `DOMPurify.sanitize`，无缓存 → O(消息数 × token 数)。**修复**：渲染结果按 `msgId` 缓存，或抽子组件 + `v-memo`。

### H-3′. 消息列表用索引作 key（UI 层）

`AiChatView.vue` / `AiPanel.vue` / `DiffView.vue` 用 `:key="i"`；列表中间删除/插入时 DiffView 的内部勾选状态（`toggledOff`）串到别的消息。**修复**：改用稳定 `msg.id`。

### H-1R. 同步 command 在主线程做重 IO（Rust）

`export_epub`、`export_project`、`write_file`、`import_files`、`empty_trash` 等非 async 命令默认跑在 UI 线程，大项目导出/保存时窗口 ANR。作者只在 `list_dir` 用了 `spawn_blocking`（正确示范），其余未跟进。**修复**：全部改为 `async fn` + `spawn_blocking`。

### H-2R. `list_dir` 递归跟随符号链接/junction（Rust）

`path.is_dir()` 对 symlink 返回 true，递归会跟随：指向父目录 → 无限递归；指向项目外 → 外部文件内容被读入字数统计。**修复**：递归前 `symlink_metadata` 判 `is_symlink()`，跳过链接。

### H-3R. CSP `connect-src` 过宽（配置）

`connect-src https: http: ws: wss:` 允许向任意端点外发。**修复**：收紧到具体 AI API 域名白名单，保留 `ipc:` 与本地开发源。

### H-仓库. 工作区含 40 个文件、+5149/−1661 行的未提交改动

含整个 AI 对话特性、回收站、多个新组件与测试，且 `.github/`、`.dsh_better_edit/` 未提交/未忽略。单次提交体量过大，有丢失风险，建议拆分为多个逻辑提交。

---

## 4. Medium（摘选）

**正确性/数据**
- **M-2 温度不持久化**：`settings.ts` 的 watch 依赖缺 `aiTemperature`，单独拖温度滑块不落盘，重启回 0.8。
- **M-4 快速切换文件竞态**：`handleFileSelect` 无请求序号，A→B 快速点击时最终激活文件由异步完成顺序决定。
- **M-5 行距默认值 bug**：`Number(null) === 0` 使首次用户拿到 1.5 而非意图的 1.8。
- **M-3 删除/恢复与备份不联动**：删到回收站不迁移 `.backups` 历史，恢复后旧备份失联。
- **M-4 备份匹配 `ends_with` 脆弱**：`1.md` 与 `第1.md` 的备份互相误判。
- **M-5 同秒保存覆盖备份**：备份时间戳粒度为秒。
- **M-1 字数口径虚高**：CJK 标点区（U+3000–303F）与全角区（U+FF00–FFEF）计入字数，中文标点占比 10–20% 被算成字；Rust 与 TS 两端一致但口径与主流写作软件不符。
- **M-2R 字典序排序**：Rust 端 `第10章 < 第2章`（前端有 Intl.Collator 数字排序兜底，需确认展示链路实际生效）。
- **L-10 应用 AI Diff 不计入今日字数**：`handleApplyDiff` 未调 `stats.recordChapterEdit`。

**安全**
- **M-1 请求头/请求体可能不一致**：`buildAiHeaders()` 忽略参数重读 store，切换 provider 瞬间可能把 A 的 key 发到 B 的端点。
- **M-3 明文 key 回传 `save_ai_config`**：前端每次加载/保存都把 keyring 读回的明文 key 塞回 aiConfig 再传后端，安全押注 Rust 端剥离单点（Rust 端 `save_ai_config` 确有剥离，已核实）。
- **M-6R `get_ai_config` 不剥离旧格式明文 key**（已抽查验证）：config.json 若为旧版本遗留，明文 key 会原样回传前端。
- **L-3 错误响应体原文（≤300 字）落盘聊天记录**；**L-5 测试连接向任意自配端点发送真实 key**（建议 scheme 校验 + 提示）。

**性能**
- **M-6 外部拖入无总量上限**：单文件限 100MB，但整目录拖入全部读进内存再 base64（+33%），可 OOM。
- **L-5R 排序比较器反复 `to_lowercase`** 分配。

**UI/可访问性/主题**
- **M1** 弹窗普遍缺 Escape 关闭与焦点管理（TrashModal 只能点按钮关）；**M2** 纯图标按钮缺 `aria-label`；**M3** 流式期间不自动跟随滚动；**M4** 收藏分组折叠状态从不写回 localStorage（注释称"跨会话记忆"）；**M5** WritingCalendar 硬编码灰阶/翠绿绕过主题系统；**M6** 弹窗遮罩色 6 处硬编码且 0.08–0.3 不一致；**M7** 设置页写作 section 重复渲染 + `ml-13` 非标准类名；**M8** 星标黄/达标绿等魔法色散落多处。

---

## 5. Low（摘选）

- Rust：生产路径 `unwrap()` 可 panic（时钟早于 1970）；IO 错误原文（含本机绝对路径）透传前端；`eprintln!` 调试日志残留（拖出路径无条件输出）；`sanitize_component` 漏 Windows 保留名（CON/PRN/NUL）与结尾点；注释与实现不符（撤销回原样并不跳过备份）；`time` crate 死依赖；Cargo.toml license/repository 为空。
- 前端：`contextLabel` 双重赋值（"已截断"永不显示）；`renderMarkdown` 块级转换泄漏进代码块；`countWords` 标点计字（与 Rust 一致的既有口径）；快捷键仅 `ctrlKey` 无 Mac `metaKey`；`deletePath` 后不持久化标签；`useProjectOperations` store 与 props 双重写入；`any` 集中在 JSON 边界无运行时校验。
- UI：`nextTick` 未用导入；死 CSS 变量（`--accent` 等 5 个无消费方）；MdEditor 3 个定时器未在卸载时清理；保存状态乐观更新不校验落盘结果（写失败仍显示"已保存"）；外部修改会污染撤销历史（无 `addToHistory: false`）；FileTreeNode 目录/文件两分支模板重复；嵌套交互元素（button 内嵌 button）；Windows 下文件树不可拖拽（`draggable="!isWindows"`）无 UI 说明。

---

## 6. 仓库卫生（独立发现）

1. 根目录 AI 代理残留：`~/.config/alma/skills/.../SKILL.md`（0 字节）、`nul`（0 字节，Windows 重定向误写产物）、`.alma-snapshots/`（996 项）、`.deepseek/`。前两者已被 .gitignore 覆盖，但 `.dsh_better_edit/` **未忽略**，会出现在 `git status`。
2. **无 `.gitattributes`**：git 对每个文件报 LF→CRLF 警告，行尾风格不一致风险。
3. `build.ps1` / `setup.ps1` 硬编码本机路径（`C:\Users\21172\.cargo\bin`、`C:\nvm4w\nodejs`），不可移植。
4. `index.html` `<title>` 仍为"写作助手"，与更名后的"砚砚"不一致。
5. 测试覆盖仅 13 用例（2 文件）：保存竞态、SSE 解析、markdown 渲染、聊天落盘等核心逻辑零覆盖（对应 M-7）。
6. 构建产物 `dist/` 已正确 gitignore；`public/` 与 `src/assets/` 图标重复（favicon.svg、icons.svg 两处各一份）。

---

## 7. 做得好的地方

**Rust 后端**
- `write_file` 原子写（同目录临时文件 + rename），崩溃不留半截文件；
- 备份去重（fnv1a hash）、备份先于覆盖执行，语义正确；
- `export_epub` zip 条目全硬编码，**无 zip-slip**；`mimetype` 首条 Stored 不压缩；章节名/正文 XML 转义完整；
- keyring 用法正确（service + provider 条目），`save_ai_config` 剥离明文密钥；删除失败不阻塞；
- 未启用 shell/fs 危险插件，capabilities 最小集（core:default + dialog:default + allow-destroy）；
- `reveal_path` 用 `Command::arg()` 单参数传递，无 shell 注入面；
- `list_dir` 正确使用 `spawn_blocking`，避免首次加载卡 UI。

**前端逻辑层**
- contentProvider「零拷贝按需读取」——每文件撤销历史 + 避免整篇拷贝；
- AbortController 新旧请求守卫、SearchModal `searchSeq` 防旧结果覆盖——竞态意识在多数地方到位；
- XSS 链路闭合：三条 v-html 路径均有转义/DOMPurify；API Key 不进日志、不进聊天记录；
- diff 大文档自动降级（Uint32Array）、chat 落盘防抖、`Intl.Collator` 数字感知排序。

**UI 层**
- 组件分层清晰，弹层 `defineAsyncComponent` 按需加载，状态收敛 Pinia；
- CodeMirror 每文件状态缓存 + 零拷贝内容提供者是亮点设计；
- 中文注释完整、可读性好，无 TODO/FIXME 残留。

---

## 8. 修复优先级建议

**P0（先修，涉及数据安全/丢失）**
1. C1：统一路径沙箱 `resolve_within`（Rust，30 个命令收口）
2. H-1：`handleSave` 保存竞态丢字
3. H-2：关标签前校验保存结果
4. H-1′：MdEditor 切换令牌竞态守卫
5. C2：`delete_path` 根目录改为后端 config 读取

**P1（安全纵深 + 明显体验）**
6. H-1R：重 IO 命令改 async + spawn_blocking
7. H-2R：list_dir symlink 防护
8. H-3R：CSP connect-src 白名单
9. M-6R：`get_ai_config` 读侧剥离旧明文 key
10. M-1/M-2/M-3：AI 请求头一致性、温度持久化、前端主动剥离 key
11. UI H-2′/H-3′：消息渲染缓存 + 稳定 key

**P2（按排期）**
12. 字数口径（M-1）、自然排序确认、备份联动（M-3/M-4/M-5）
13. 可访问性（Escape/aria/焦点）、主题一致性（M4–M8）
14. 仓库卫生：删残留文件、补 `.gitattributes`、提交拆分的未提交改动、脚本去硬编码、补核心逻辑测试（M-7）

---

## 9. 总体评价

| 维度 | 评分 | 说明 |
| --- | --- | --- |
| 架构 | A- | 前后端职责清晰、分层合理、composable 抽取到位 |
| 代码质量 | B+ | 注释质量高、无死 TODO，但大文件集中（lib.rs 1309 行、App.vue/MdEditor/SettingsContent 均 20KB+） |
| 正确性 | B | 存在 1 个数据丢失竞态 + 若干"异步返回后状态覆盖"类小 bug |
| 安全 | B- | 前端 XSS/密钥面闭合良好；**Rust IPC 无路径沙箱是最大短板**（纵深防御缺失） |
| 测试 | C | 仅 13 用例，核心逻辑零覆盖 |
| 仓库卫生 | C+ | 未提交改动堆积、代理残留、无 .gitattributes |

**综合评级：B+**。这是一个架构思路清晰、工程意识良好的个人/小团队级桌面应用，作者在原子写、备份、XSS、keyring 等方面有明显安全意识。首要风险不是功能 bug，而是 **IPC 层信任边界缺失**（C1/C2）与**保存路径的竞态丢字**（H-1/H-1′），建议按 P0 清单优先处理。
